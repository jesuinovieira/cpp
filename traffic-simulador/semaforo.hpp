#ifndef SEMAFORO_HPP
#define SEMAFORO_HPP

/*

    ????????????????????????????????????????????????????????????????????????
    Cada semáforo possui um vetor das filas que fazem parte dele, dividindo-
as em eferentes (saída) e aferentes (entrada).

*/

class Semaforo
{
public:
    Semaforo(int = 10);

    void trocaEstado(bool);
    bool getEstado() const;

private:
    bool _estado;           // Aberto = true ou Fechado = false
    const int _tempo;       // Tempo que o semaforo fica aberto
};

#endif // SEMAFORO_HPP
